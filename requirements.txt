# The deterministic runtime (aml_runtime/runtime.py, semantic/, behaviour/)
# is stdlib-only. These are needed only to run the ML benchmark modules
# (semantic_benchmark, behaviour_benchmark, window_study, semantic_vs_raw,
# ml_benchmark, runtime_first_cascade, runtime_first_v2) and their tests.
numpy==2.3.1
scikit-learn==1.9.0
xgboost==3.3.0
lightgbm==4.7.0
catboost==1.2.10
matplotlib==3.10.8
pytest==9.0.3
